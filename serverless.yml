service: prettier-html

provider:
  name: aws
  runtime: nodejs6.10

functions:
  prettier:
    handler: dist/handler.prettify
    events:
      - http:
          path: prettier
          method: post
          cors: true
